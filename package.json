{
  "name": "autoreplybot-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel serverless API: Meta token sync + scheduled Facebook/Instagram posting (Firebase + OpenAI)",
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev",
    "lint": "node --check api/cron/daily-post.js"
  },
  "devDependencies": {
    "vercel": "^41.4.1"
  },

  "dependencies": {
    "firebase-admin": "^13.0.2",
    "luxon": "^3.5.0"
  }
}
